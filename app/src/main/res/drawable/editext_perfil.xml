<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Cor de fundo -->
    <solid android:color="#D3E0D1" />

    <!-- Borda -->
    <stroke
        android:width="1dp"
        android:color="#A8BFA8" />

    <!-- Cantos arredondados -->
    <corners android:radius="12dp" />

    <!-- Espaçamento interno -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />

</shape>